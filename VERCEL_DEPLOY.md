# تعليمات نشر المشروع على Vercel

## ما تم إنجازه ✅

1. ✅ تحديث `vercel.json` لاستخدام المسارات الصحيحة
2. ✅ تعطيل WebSocket/Channels على Vercel (غير مدعوم في الخطة المجانية)
3. ✅ تحديث `settings.py` لتعطيل Channels تلقائياً على Vercel
4. ✅ تحديث `asgi.py` للعمل بدون WebSocket على Vercel
5. ✅ تحديث `.gitignore` لاستبعاد الملفات غير الضرورية
6. ✅ رفع الكود إلى GitHub

## خطوات النشر على Vercel

### الطريقة الأولى: النشر عبر Dashboard (الأسهل) 🌐

1. اذهب إلى [vercel.com](https://vercel.com) وسجل الدخول
2. اضغط على "Add New Project"
3. اختر المستودع `think_spark_api` من GitHub
4. Vercel سيكتشف تلقائياً إعدادات Django من `vercel.json`
5. أضف متغيرات البيئة (Environment Variables) من `env.txt`:
   - SECRET_KEY
   - DEBUG=False
   - CLOUDINARY_CLOUD_NAME (إن وجد)
   - CLOUDINARY_API_KEY (إن وجد)
   - CLOUDINARY_API_SECRET (إن وجد)
   - أي متغيرات أخرى تحتاجها
6. اضغط "Deploy"

### الطريقة الثانية: النشر عبر CLI 💻

```bash
# تسجيل الدخول
vercel login

# النشر
vercel

# أو للنشر مباشرة للإنتاج
vercel --prod
```

## ملاحظات مهمة ⚠️

1. **قاعدة البيانات**: حالياً المشروع يستخدم SQLite في الذاكرة على Vercel، مما يعني أن البيانات ستضيع عند كل إعادة نشر. للحل الدائم، استخدم PostgreSQL (مثل Supabase أو Neon).

2. **WebSocket**: تم تعطيل WebSocket لأن Vercel Free Plan لا يدعمه. وظائف الدردشة عبر HTTP ستعمل بشكل طبيعي.

3. **المتغيرات البيئية**: تأكد من إضافة جميع المتغيرات المطلوبة في Vercel Dashboard تحت Settings > Environment Variables.

4. **Static Files**: تم إعداد WhiteNoise لخدمة الملفات الثابتة.

## بعد النشر 🚀

بعد النشر الناجح، ستحصل على رابط مثل:
`https://your-project-name.vercel.app`

يمكنك اختبار API endpoints مثل:
- `https://your-project-name.vercel.app/api/token/`
- `https://your-project-name.vercel.app/auth/`

## استكشاف الأخطاء 🔧

إذا واجهت مشاكل:
1. تحقق من logs في Vercel Dashboard
2. تأكد من أن جميع المتغيرات البيئية موجودة
3. تحقق من أن `vercel.json` صحيح
4. تأكد من أن `requirements.txt` محدث

